# Auto-close secret scanning alerts found in these paths.
# Test fixtures may contain realistic-looking but non-sensitive strings
# (HTML snapshots from Luma, mock API responses, etc.) that would otherwise
# generate noise. Real application code is not excluded.
paths-ignore:
  - "tests/fixtures/**"
